var app = app || {};

app.controller = (function() {
    function Controller() {
        
    }

    return {
        load: function() {
            return new Controller();
        }
    }
}());